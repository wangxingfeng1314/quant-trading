# A股量化交易系统 — 项目指令

## 项目概要

基于 Streamlit + pandas 的自选股中心化 A 股量化系统。  
**核心约束：所有功能以自选股（watchlist）为中心，只操作有数据的股票。**

- 项目路径: `E:/wxf/claude/quant-trading/`
- 技术栈: Streamlit + 自研 pandas 回测引擎 + AKShare/Tushare/Baostock + SQLite + Plotly
- 版本: v0.3.0

---

## 快速启动

```bash
cd E:/wxf/claude/quant-trading
py run.py    # 启动系统，浏览器自动打开 http://localhost:8501
```

---

## 数据模型（自选股中心）

| 表 | 说明 |
|:---|:------|
| `stock_basic` | 全市场 5530 只股票基本信息（名称/行业/ST标记） |
| `daily_price` | **只存自选股的日线数据**（当前 6 只，约 7245 条） |
| `watchlist` | 自选股列表（添加自动下载数据+扫信号） |
| `signal` | 交易信号 |
| `backtest_result` | 回测结果 |
| `backtest_trade` | 回测交易明细 |
| `index_daily` | 大盘指数日线（上证/深证/创业板） |
| `position` | 模拟持仓（持久化） |

**关键架构决策：系统已从"全市场扫描"改为"只看自选股"。**  
所有功能页面只操作有数据的股票，加自选股自动就绪。

---

## 功能页面（7个）

| 页面 | 功能 | 说明 |
|:-----|:------|:------|
| 🏠 **首页看板** | 大盘指数、自选股快照（趋势/RSI/MA20）、信号排行、**数据健康面板** | 一屏掌握全局 |
| 📈 **数据浏览** | K线图+成交量+MA/MACD/布林带/RSI | 默认只列出有数据的股票 |
| 🔬 **回测中心** | 单次回测/网格搜索(串行+并行)/多策略对比/沪深300基准/归因分析 | 支持导出报告 |
| 🔍 **选股筛选** | 技术因子筛选（均线/MACD/RSI/布林带/KDJ/放量） | 按综合评分排序+导出CSV |
| 📡 **信号中心** | 实时扫描(进度条)/历史信号/信号验证后N日/组合信号 | 扫描范围自选股优先 |
| 💼 **持仓管理** | 自选股管理+分组/模拟持仓(持久化)/信号自动跟单 | K线买卖点标注 |
| 📚 **策略百科** | 策略说明+参数+历史回测排行榜 | 静态页 |

---

## 策略（11个，按风格分类）

### 趋势跟踪（4个）
| 策略 | 文件 | 说明 |
|:-----|:-----|:------|
| 双均线交叉 | `ma_cross.py` | 金叉买/死叉卖 |
| 均线多头排列 | `ma_bullish.py` | MA5>MA20>MA60 入场 |
| 海龟突破 | `turtle.py` | 突破N日高点买/跌破低点卖 |
| 唐奇安通道突破 | `donchian_breakout.py` | 通道突破+ATR止损 |

### 反转交易（4个）
| 策略 | 文件 | 说明 |
|:-----|:-----|:------|
| MACD背离 | `macd_divergence.py` | 底背离买/顶背离卖 |
| RSI超买超卖 | `rsi_oversold.py` | RSI<30买/>70卖 |
| 布林带反转 | `bollinger_reversal.py` | 触下轨买/触上轨卖 |
| 双底形态识别 | `double_bottom.py` | 自动检测W底突破颈线 |

### 动量（2个）
| 策略 | 文件 | 说明 |
|:-----|:-----|:------|
| KDJ金叉死叉 | `kdj_cross.py` | 低位金叉买/高位死叉卖 |
| 量价突破 | `volume_price_breakout.py` | 放量突破均线买/缩量反弹卖 |

### 组合（1个）
| 策略 | 文件 | 说明 |
|:-----|:-----|:------|
| 多因子综合评分 | `multi_factor.py` | 5因子打分（均线+MACD+RSI+量能+布林） |

**添加新策略：** `strategies/` 下新建文件 → 继承 `BaseStrategy` → `strategies/__init__.py` 注册 → 重启即识别。

---

## 数据命令

```bash
# 首次初始化（只下载自选股数据）
py scripts/init_data.py --watchlist

# 增量更新（只更新自选股，覆盖最近14天）
py scripts/init_data.py --update --days 30 --watchlist

# 全市场模式
py scripts/init_data.py --stocks 300
py scripts/init_data.py --stocks 300 --resume  # 断点续传
```

Windows 定时任务 `QuantTrading-DataUpdate`，每个工作日 17:00 自动更新。  
UI 侧边栏也提供「更新自选股数据」按钮（带进度条）。

---

## 数据源级联

| 优先级 | 数据源 | 用途 | Token | 熔断保护 |
|:------:|:-------|:-----|:-----:|:---------|
| 1 | **AKShare** 🏆 | 日线（前复权）+ 指数 + 成分股 | ❌ | 连续20次失败→熔断300s |
| 2 | Tushare Pro | 股票列表（含行业）+ 复权因子 | ✅ 可选 | — |
| 3 | Baostock | 日线最后兜底 | ❌ | — |

### 重试策略
- **AKShare**：单只最多重试 3 次，指数退避（2s→4s→8s）+ 随机抖动
- **Tushare**：2 次重试 + 限频 0.35s
- **指数获取**：2 次重试
- 全部失败 → `logger.warning`（升级前是 `debug`，用户可见）

---

## 回测引擎特性

- **A股费用模型**：佣金万2.5（最低5元）、印花税万5（仅卖出）、过户费万0.1、滑点千1
- **涨跌停限制**：主板±10%、ST±5%、创业板/科创板±20%
- **T+1 规则**：买入当日不可卖出
- **评分仓位管理**：score=0.5→5%仓位，score=1.0→20%仓位
- **基准对比**：沪深300归一化曲线
- **网格搜索**：`grid_search()`（串行）+ `grid_search_parallel()`（并行，多核加速）
- **绩效指标**：总收益/年化/最大回撤/夏普/卡玛(Calmar)/Alpha/Beta/胜率
- **绩效归因**：月度收益热力图 + 逐年收益 + 滚动夏普/回撤

---

## 消息推送（4通道）

| 通道 | 环境变量 | 说明 |
|:-----|:---------|:------|
| Server酱 | `SERVER_CHAN_KEY` | https://sct.ftqq.com |
| PushPlus | `PUSHPLUS_TOKEN` | https://www.pushplus.plus |
| 企业微信机器人 | `WECOM_WEBHOOK` | 群机器人 Webhook URL |
| 钉钉机器人 | `DINGTALK_WEBHOOK` + `DINGTALK_SECRET` | 支持加签模式 |

自动选择已配置的通道，至少一个成功即返回 True。

---

## 配置项（.env）

```
TUSHARE_TOKEN=xxx              # 可选，AKShare不需要
SLIPPAGE_RATE=0.001            # 滑点比率
DATA_START_DATE=20210701       # 数据起始日期
DEFAULT_CAPITAL=100000         # 回测默认资金
COMMISSION_RATE=0.00025        # 佣金万2.5
STAMP_TAX_RATE=0.0005          # 印花税万5
SCHEDULER_ENABLED=true         # 定时任务
SCHEDULER_HOUR=17              # 17:00执行
WECOM_WEBHOOK=                 # 企业微信 Webhook（可选）
DINGTALK_WEBHOOK=              # 钉钉 Webhook（可选）
DINGTALK_SECRET=               # 钉钉加签密钥（可选）
```

---

## 测试（89个单元测试 + 集成测试 + 对齐测试）

```bash
py -m pytest tests/ -v         # 运行全部 89 个测试
py -m pytest tests/ -v --tb=short  # 简略回溯
```

| 测试文件 | 测试数 | 覆盖内容 |
|:---------|:------|:---------|
| `test_models.py` | 9 | Signal/Trade/BacktestResult/StockInfo 模型 |
| `test_commission.py` | 15 | 费用计算/滑点/涨跌停/取整手 |
| `test_portfolio.py` | 14 | 买入卖出/权益/指标/边界 |
| `test_backtester.py` | 11 | 回测主循环/指标/网格搜索 |
| `test_backtester_integration.py` | 5 | 真实 cleaner+indicators 集成 |
| `test_storage.py` | 24 | 7张表 CRUD 全覆盖 |
| `test_schema_alignment.py` | 11 | 模型-数据库表结构对齐 |

---

## CI/CD

GitHub Actions 配置在 `.github/workflows/ci.yml`，每次 push/PR 自动：
1. ✅ 语法检查（py_compile 全量扫描）
2. ✅ pytest 49 个测试
3. ✅ 关键模块 import 一致性检查

---

## 系统架构

```
quant-trading/
├── app/                    # Streamlit 7个页面
│   ├── main.py             # 主入口 + 侧边栏
│   ├── dashboard.py        # 🏠 首页看板（含数据健康面板）
│   ├── backtest.py         # 🔬 回测中心
│   ├── signal.py           # 📡 信号中心
│   ├── portfolio.py        # 💼 持仓管理
│   └── screener.py         # 🔍 选股筛选
├── core/                   # 配置 + 模型
├── data/                   # 数据层
│   ├── fetcher.py          # 多源级联 + 重试 + 熔断
│   ├── storage.py          # SQLite CRUD
│   ├── indicators.py       # 技术指标
│   └── cleaner.py          # 数据清洗
├── engine/                 # 回测引擎
│   ├── backtester.py       # 回测 + 网格搜索（串行/并行）
│   ├── portfolio.py        # 组合管理
│   ├── commission.py       # A股费用模型
│   └── scanner.py          # 信号扫描器
├── strategies/             # 11个策略（插件式）
├── scripts/                # 运维脚本
├── scheduler/              # 定时调度
├── notifier/               # 消息推送（4通道）
├── tests/                  # 49个单元测试
├── .github/workflows/      # CI/CD
├── CHANGELOG.md
├── requirements.txt        # 版本锁定
└── data/quant.db           # SQLite
```

---

## 历史问题（已修复陷阱）

### 2026-07-15
- `update_data.bat` → LF→CRLF + 中文路径改用 `%USERPROFILE%`
- `backtest.py` → `st.number_input` 去掉 `min_value=0.0` 限制
- `models.py` + `backtester.py` → `BacktestResult` 新增 `calmar_ratio`
- `backtest.py` → `_draw_monthly_heatmap` 中 `int(row["month"])` 修复 numpy.float64 索引
- `storage.py` → `save_signal` 改用 `INSERT OR REPLACE`

### 2026-07-27（第一轮 — 质量与工程化提升）
- `scanner.py` → 静默异常: debug→warning 级别
- `fetcher.py` → Tushare/指数加自动重试; 全源失败升级 warn
- `tests/` → 从零到 49 个单元测试（模型/费用/组合/回测/网格）
- `requirements.txt` → 版本锁定
- `.github/workflows/ci.yml` → CI/CD 配置
- `dashboard.py` → 数据健康监控面板（5卡片 + 数据源状态 + 健康度明细）
- `backtester.py` → 新增 `grid_search_parallel()` 并行网格搜索
- `push.py` → 新增企业微信机器人 + 钉钉机器人通道
- `CHANGELOG.md` → 创建更新日志
- `.atomcode.md` → Git 提交规范

### 2026-07-27（第二轮 — 架构与测试补全）
- `cleaner.py` → 修复 L84 索引越界 bug（`ratio[is_old].index`）
- `storage.py` → 加 SQLite `timeout=5.0` + 文件锁 `update_lock()` 防双写
- `scheduler/__init__.py` → 定时任务自动扫信号+推送通知（打通闭环）
- `app/backtest.py` → 新增 `_show_nlp_summary()` 回测自然语言结论
- `tests/test_backtester_integration.py` → 5 个集成测试（真实 cleaner+indicators）
- `tests/test_storage.py` → 24 个 storage CRUD 测试（7张表全覆盖）
- `app/portfolio.py` → 删除自选股二步确认（点移除→点确认→toast）
- `strategies/__init__.py` → importlib 自动发现策略，无需手动注册
- `core/models.py` → BacktestResult 新增 `sell_count` 字段
- `.python-version` → 指定 Python 3.11

### 2026-07-28（第三轮 — 稳定性和日常使用优化）
- `scripts/update_data.bat` → Windows 计划任务也走 `update_lock` 文件锁
- `app/main.py` → 启动时调用 `init_db()` 确保数据库迁移执行 + `check_db_integrity()` 完整性检查
- `data/storage.py` → 新增 `check_db_integrity()` + `_TokenBucket` 令牌桶限速
- `notifier/push.py` → 新增 `notify_position_summary()` 每日持仓盈亏推送
- `engine/scanner.py` → 信号扫描缓存（当天已扫过的直接从数据库返回）
- `tests/test_schema_alignment.py` → 11 个模型-数据库表结构对齐测试
- `tests/` 总数从 78 → **89**

---

## Git 提交规范

```
<type>(<scope>): <subject>
```

| Type | 说明 |
|:-----|:------|
| `feat` | 新功能 |
| `fix` | Bug 修复 |
| `perf` | 性能优化 |
| `test` | 测试相关 |
| `docs` | 文档/注释 |
| `refactor` | 重构 |
| `style` | 格式调整 |
| `chore` | 构建/CI/工具链 |

---

## 开发注意

- Python 3.11，Windows 下用 `py` 命令
- `.gitignore` 屏蔽 `__pycache__/`、`*.db`、`.env`、`logs/`
- 日志轮转：5MB × 3 份
- 所有 DB 操作只过 `data/storage.py`
