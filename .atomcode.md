# A股量化交易系统 — 项目指令

## 项目概要

基于 Streamlit + pandas 的自选股中心化 A 股量化系统。  
**核心约束：所有功能以自选股（watchlist）为中心，只操作有数据的股票。**

- 项目路径: `E:/wxf/claude/quant-trading/`
- 技术栈: Streamlit + 自研 pandas 回测引擎 + AKShare/Tushare/Baostock + SQLite + Plotly
- 版本: v0.3.0

---

## 快速启动

```bash
cd E:/wxf/claude/quant-trading
py run.py    # 启动系统，浏览器自动打开 http://localhost:8501
```

---

## 数据模型（已改为自选股中心）

| 表 | 说明 |
|:---|:-----|
| `stock_basic` | 全市场5530只股票基本信息（名称/行业/ST标记） |
| `daily_price` | **只存自选股的日线数据**（当前5只，约6093条） |
| `watchlist` | 自选股列表（添加自动下载数据+扫信号） |
| `signal` | 交易信号 |
| `backtest_result` | 回测结果 |
| `index_daily` | 大盘指数日线（上证/深证/创业板，3657条） |

**关键变更：系统已从"全市场扫描"改为"只看自选股"。**  
所有功能页面只操作有数据的股票，加自选股自动就绪。

---

## 功能页面（7个）

| 页面 | 功能 | 关键改动 |
|:-----|:-----|:---------|
| 🏠 **首页看板** | 大盘指数、自选股快照（趋势/RSI/MA20）、信号、自选股排行 | 系统状态显示自选股数，非全市场 |
| 📈 **数据浏览** | K线图+成交量+MA/MACD/布林带/RSI | 默认只列出有数据的股票 |
| 🔬 **回测中心** | 单次回测/网格搜索/多策略对比/沪深300基准对比 | 股票下拉框只显示有数据的股票 |
| 🔍 **选股筛选** | 技术因子筛选（均线/MACD/RSI/布林带/KDJ/放量） | 自动只扫描有数据的股票 |
| 📡 **信号中心** | 实时扫描（含进度条）/历史信号/信号验证 | 扫描范围默认"自选股(快)" |
| 💼 **持仓管理** | 自选股管理/模拟持仓/信号自动跟单 | 加自选股→自动下载数据→自动扫信号 |
| 📚 **策略百科** | 策略说明+参数+历史回测排行榜 | 静态页面 |

---

## 策略（11个，按风格分类）

### 趋势跟踪
| 策略 | 文件 | 说明 |
|:-----|:-----|:------|
| 双均线交叉 | `ma_cross.py` | 金叉买/死叉卖 |
| 均线多头排列 | `ma_bullish.py` | MA5>MA20>MA60 入场 |
| 海龟突破 | `turtle.py` | 突破N日高点买/跌破低点卖 |
| 唐奇安通道突破 | `donchian_breakout.py` | 通道突破+ATR止损 |

### 反转交易
| 策略 | 文件 | 说明 |
|:-----|:-----|:------|
| MACD背离 | `macd_divergence.py` | 底背离买/顶背离卖 |
| RSI超买超卖 | `rsi_oversold.py` | RSI<30买/>70卖 |
| 布林带反转 | `bollinger_reversal.py` | 触下轨买/触上轨卖 |
| 双底形态识别 | `double_bottom.py` | 自动检测W底突破颈线 |

### 动量
| 策略 | 文件 | 说明 |
|:-----|:-----|:------|
| KDJ金叉死叉 | `kdj_cross.py` | 低位金叉买/高位死叉卖 |
| 量价突破 | `volume_price_breakout.py` | 放量突破均线买/缩量反弹卖 |

### 组合
| 策略 | 文件 | 说明 |
|:-----|:-----|:------|
| 多因子综合评分 | `multi_factor.py` | 5因子打分（均线+MACD+RSI+量能+布林） |

**添加新策略：** `strategies/` 下新建文件 → `strategies/__init__.py` 注册一行 → 重启即识别。

---

## 数据命令

```bash
# 首次初始化（只下载自选股数据）
py scripts/init_data.py --watchlist

# 增量更新（只更新自选股，覆盖最近14天）
py scripts/init_data.py --update --days 30 --watchlist

# 侧边栏「更新自选股数据」按钮（UI方式，同上）
```

Windows 定时任务 `QuantTrading-DataUpdate`，每个工作日17:00自动更新。

---

## 关键代码改动（本会话）

### 2026-07-15 Bug修复
- `scripts/update_data.bat` → 修复两个问题: ① LF→CRLF换行符（cmd.exe无法解析LF）② 硬编码中文路径`C:\Users\王兴锋\...`改为`%USERPROFILE%`（UTF-8编码的bat文件在cmd.exe下中文路径乱码）
- 删除Windows定时任务 `A股量化数据更新`（18:00），保留 `QuantTrading-DataUpdate`（17:00工作日）
- `app/backtest.py` → 修复`st.number_input`硬编码`min_value=0.0`导致多因子策略sell_threshold=-3.0报错，改为去掉min_value限制
- `core/models.py` + `engine/backtester.py` → `BacktestResult`新增`calmar_ratio`字段，修复回测结果显示卡玛比率时`AttributeError`
- `app/backtest.py` → `_draw_monthly_heatmap`中`mi = int(row["month"]) - 1`，修复groupby后month为numpy.float64不能索引numpy数组的`IndexError`
- `data/storage.py` → `save_signal`中`INSERT`改为`INSERT OR REPLACE`，修复重复扫描信号时`UNIQUE constraint failed`

### 自选股中心化适配
- `storage.py` → 新增 `get_stocks_with_data(min_days)` 辅助函数
- `init_data.py` → 新增 `--watchlist` 参数（初始下载+增量更新只针对自选股）
- `run_update()` → 新增 `watchlist=True` 参数，新增 `progress_callback` 支持
- `update_data.bat` / `scheduler/__init__.py` → 定时任务全加 `--watchlist`

### 更新逻辑变更
- 增量更新：**最近14天的数据每次都重新拉取覆盖**（改前：有今天数据就跳过）
- 侧边栏按钮：`run_update(days=14, watchlist=True)`，带进度条+实时文字

### 页面适配（共11处改动）
| 文件 | 改动 |
|:-----|:------|
| `main.py` | 侧边栏状态显示自选股数 + 一键更新(进度条) + 定时任务状态 + 数据库备份/恢复 |
| `dashboard.py` | 系统状态/热门股票全改为自选股视角 + 数据健康度 + 每日复盘报告 |
| `backtest.py` | 股票选择只显示有数据的股票 + 批量回测自选股 + 矩阵热力图 + 策略参数模板 |
| `screener.py` | 去掉"前N只"输入，改为可扫数量只读提示 |
| `signal.py` | 扫描范围改为"有数据的股票"/"自选股(快)" + 组合信号合成 |
| `portfolio.py` | 加自选股→自动下载数据→自动扫信号 + 分组管理 + 模拟持仓持久化 |
| `data_viewer.py` | 默认只列出有数据的股票 |
| `signal.py` | 修复变量名 BUG（df→df_results） |
| `backtest.py` | 修复 f-string 缺 f 前缀 BUG |
| `strategy_intro.py` | 补全4个新策略的使用建议 |

### 新增功能清单
- 11个策略（趋势跟踪4+反转4+动量2+组合1）
- 策略参数模板保存/加载 + 标记当前参数
- 组合信号合成（多策略共识分析）
- 数据库一键备份/恢复
- 回测绩效指标增强（Calmar/Alpha/Beta）
- 自选股分组（长线池/短线池）
- 每日复盘报告生成+导出
- 数据更新进度条 + 实时文字
- 定时任务状态可视化
- 模拟持仓持久化到SQLite
- 信号通知推送（已接入push.py）

---

## 配置项（.env）

```
TUSHARE_TOKEN=xxx           # 可选，AKShare不需要
SLIPPAGE_RATE=0.001         # 滑点比率
DATA_START_DATE=20210701    # 数据起始日期
DEFAULT_CAPITAL=100000      # 回测默认资金
COMMISSION_RATE=0.00025     # 佣金万2.5
STAMP_TAX_RATE=0.0005       # 印花税万5
SCHEDULER_ENABLED=true      # 定时任务
SCHEDULER_HOUR=17           # 17:00执行
```

---

## 开发注意

- Python 3.11，Windows 下用 `py` 命令
- `.gitignore` 屏蔽 `__pycache__/`、`*.db`、`.env`、`logs/`
- 日志轮转：5MB × 3 份
- 所有 DB 操作只过 `data/storage.py`
